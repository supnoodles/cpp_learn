#include <iostream>
#include <vector>

using namespace std;

int main()
{

    vector<int> v = {1, 2, 3, 4};


    // using the prefix op, it would start at 2, and finish at one past our vector.
    // auto pbeg = v.begin();
    // // print elements up to the first negative value
    // while (pbeg != v.end() && *pbeg >= 0)
    //     cout << *++pbeg << endl; // print the current value and advance pbeg
}