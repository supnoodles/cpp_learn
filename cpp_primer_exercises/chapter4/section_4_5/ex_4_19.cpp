#include <iostream>
#include <vector>

using namespace std;

int main()
{

    int *ptr;
    vector<int> vec;
    int ival;

    /* 
    a) checks that pointer is not a null pointer and increments pointer,
    returning non-incremented value. 
    (Returns true if ptr is not a null pointer.)

    b) evaluats to true if ival != 0

    c) always evaluated to true.
    */
}