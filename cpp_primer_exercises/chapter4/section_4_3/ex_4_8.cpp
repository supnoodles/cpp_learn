// short circuit evaluation
/*
 && evalutates left operand before the right
 right operand of && is evaluated IFF left operand is true.

 || evaluates left operand before the right
 right operand of || is evaluated IFF left operand is false.
  */