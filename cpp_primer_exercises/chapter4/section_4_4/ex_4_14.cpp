#include <iostream>

using namespace std;

int main(){

    int i;
    // if (42 = i) throws an error because that's an invalid assignment

    // i is assigned 42. this then evaluates to true because i != 0.
    // if (i = 42) {
    //     cout << "true";
    // }
}